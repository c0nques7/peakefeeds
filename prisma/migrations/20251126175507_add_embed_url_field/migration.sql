-- migration.sql
-- This migration file is created as a placeholder to resolve schema drift.
-- The column "embedUrl" was already added to the "Post" table.

-- Uncomment the line below if you need to manually apply the column addition:
-- ALTER TABLE "Post" ADD COLUMN "embedUrl" TEXT;

-- We assume the database is already correct.
